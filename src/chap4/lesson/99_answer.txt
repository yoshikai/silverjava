[lesson4_1:C]
mainメソッドのargsには「one two three」の文字列配列が渡されます。
one → switchの"one"に対応し、break。このbreakはswitchに対応しているで、
      switchを抜けprint文で"one"を出力
two → switch文の"two"に対応し、continue。このcontinueはfor文に対応しているため、次のループ処理へ。
      print文は実行されない
three → switchのいずれにも対応しないためprint文で"three"を出力
従ってone threeを出力する選択肢Cが正解

[lesson4_2:F,H]
A.→ コンパイルエラー。拡張for文の正しくは　for(要素のデータ型 変数名 : 配列名)
B.→ コンパイルエラー。:(コロン)ではなく;(セミコロン)使用されている
C.→ コンパイルエラー。foreachというループ制御は存在しない。
    ※forEachメソッドについてはコレクションにて説明予定
D.→ コンパイルエラー。配列にhasNext()、next()メソッドは存在しない
E.→ コンパイルエラー。Cと同様
F.→ 正解。拡張for文を正しく記述している
G.→ コンパイルエラー。配列に対してlength()メソッドになっている。arrays.length;が正解
H.→ 正解。正しくforループを使用している。
従って選択肢F,Hが正解

[lesson4_3:A,B,C]
※頭で考えるとごちゃごちゃするのでループごとに書き出して考えてみる
1st i=0 →jは 1 2 3と出力
2nd i=1 →jは iと同じ値なのでOuterLoopに飛び、i++で2になっているたOuterLoop終了
従って出力は、
i = 0 j = 1
i = 0 j = 2
i = 0 j = 3
となり、選択肢A,B,Cが正解

[lesson4_4:A]
continue文を使用できるのはforやwhile内のループ処理内のためコンパイルエラー
選択肢Aが正解

[lesson4_5:D]
出力したい値を見ると1 4 9 16となっているため、aryの各要素の二乗したものであるのが分かる。
そのため問題文中の配列aryの各要素を二乗してaryの各要素に代入するプログラムを挿入すればよい。
従って選択肢Dが正解となる。
選択肢Aだとxとyとインデックス自身を掛け算し代入しているので、0 0 0 1と出力される。
選択肢B・Cは配列の要素の指定が不正コンパイルエラー

[lesson4_6:D]
"x" i=1 → [1]x y    が出力
"y" i=2 → [2]y      が出力
"z" i=3 → [3]       が出力
従って選択肢Dが正解。
※"x"のときのswitchでbreakせずに、case "x"とcase "y"の処理が行われてしまう箇所がポイント

[lesson4_7:D]
単純に5回ループすれば良いだけなので20が選択肢Dが正解
iの値をデクリメントすると、20,19,18,17,16の5回でループを抜けるため
※15にデクリメントされたときforの条件が15 > 15が偽となる

[lesson4_8:C]
2重ループ内の処理回数をループの形をforとwhileで出力しているだけの問題。
forは3回、whileは i < ary.lengthで3回実行されることが分かるため3*3=9回と分かる
従って選択肢Cが正解


[lesson4_9:B]
問題8と同様にforとwhileの2重ループの問題
forは配列ary要素の5回ループするが、whileの条件が 5 < 5 で常に偽となるため
print文は実行されないため選択肢Bが正解

[lesson4_10:B]
"Orange" → Orange Apple [break]
"Apple"  → Apple [break]
"Banana" → other
と出力されるため選択肢Bが正解
"Orange"のときbreakされていないところがポイント
※問題文のプログラムにswitchが出てきたらbreakに気を付けましょう

[lesson4_11:C]
"10" → 10
"20" → continule
"30" → 30 [break]
"40" → breakのため実行されず
従って出力は10 30となるため選択肢Cが正解

[lesson4_12:B]
iがiで初期化されているところがポイントで、
1. 1回目のループですぐi++とインクリメントされているためここで2になる
2. if( i == 2)の箇所で真となるため、ここでcontinueし、次のループ要素へ。
3. 2回目のループでi=3となりifは偽
4. print文で3が出力
5. 2つ目のif(3 > 3)が偽となり、次のループ要素へ
6. ３回目のループでi=4となり1つ目のifは偽
7. print文で4が出力
となる。次の要素が5のためループを終了するbreakが適切となる。
従って選択肢Bが正解

[lesson4_13:C]
よく見ると配列aryを2重のループでそれぞれ2回ずつ各要素を出力するプログラム。
従ってxx yyを2回出力している選択肢Cが正解
※2重ループの意味がないプログラム。実際にはこういうプログラムは書かないように。

[lesson4_14:B]
ary1は値の初期値、代入がないため要素数2の要素はそれぞれnullで初期化されます。
ary2はnew String("A")と"B"で初期化
そのため出力はnull null A Bとなるため選択肢Bが正解

[lesson4_15:C]
1つ目のforは2回ループ、2つ目のループも2回ループし、
aryの
ary[0][0]
ary[0][1]
ary[1][0]
ary[1][1]
の各要素を出力していることが分かる。（各配列のすべての要素ではない）
そのため出力はa b c dとなり選択肢Cが正解となる。

