次のコードがあります。

package A;
import java.util.*;

public class Main{
    public static void main(String[] args){
        //code more
    }
}

正しい説明は次のどれですか。１つ選択してください。

A. import文で、java.utilパッケージの特定のクラスを指定していないため、コンパイルエラーとなる
B. java.utilパッケージのすべてのクラスをインポートする
C. package文で指定するパッケージ名は、大文字は使用できないためコンパイルエラーとなる
D. package文で、Aという単体名を指定しているため、コンパイルエラーとなる
